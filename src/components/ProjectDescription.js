import React from "react";

export default class ProjectDescription extends React.Component {
  render() {
    return (
      <p>
        Provide a interface between the buyer and seller for recycle items from
        scrap
      </p>
    );
  }
}
